# سیگنال زنده — وایت‌پیپر
## بخش ۳۵: RanjOracle.sol — مشخصات کامل

**نویسنده:** امیر احمدی (@x@)
**ORCID:** 0009-0000-0614-6869
**تاریخ:** ۱۳ خرداد ۱۴۰۵
**وضعیت:** نهایی — تغییرناپذیر

---

### ۳۵.۱ هدف

`RanjOracle.sol` رابط روی زنجیره بین API اعتبارسنج وفاداری (کمیتهٔ چندمدله LLM) و قراردادهای هوشمند پروتکل است. امتیازات رنج را دریافت، اعتبارسنجی، و ثبت می‌کند.

---

### ۳۵.۲ توابع اصلی

```solidity
function submitScore(address _member, string memory _tribeSymbol, uint256 _score, bytes memory _signature) external;
function getMedianScore(address _member, string memory _tribeSymbol) external view returns (uint256);
function challengeScore(address _member, string memory _tribeSymbol) external payable;
```

---

### ۳۵.۳ اجماع چندمدله

تابع `submitScore` امتیازات را از اعتبارسنج‌های مجاز دریافت می‌کند. قرارداد ۵ امتیاز آخر را برای هر جفت عضو-تبار ذخیره می‌کند و از **میانه** به عنوان امتیاز وفاداری رسمی استفاده می‌نماید.

---

### ۳۵.۴ جریمه

اگر امتیاز یک اعتبارسنج بیش از ۳۰٪ از میانه برای ۳ دور متوالی انحراف داشته باشد، TAP استیک‌شدهٔ او جریمه می‌شود. این تضمین می‌کند که اعتبارسنج‌ها انگیزهٔ اقتصادی قوی برای دقیق بودن دارند.

---

### ۳۵.۵ اعتراض عمومی

هر عضو می‌تواند با استیک کردن ۱,۰۰۰ TAP یک امتیاز را به چالش بکشد. یک هیئت منصفهٔ تصادفی از ۷ عضو با وفاداری بالا پرونده را بررسی می‌کند. اگر اعتراض پذیرفته شود، استیک چالش‌کننده بازگردانده می‌شود و اعتبارسنج‌های خطاکار جریمه می‌شوند.

---

*در بخش بعدی، بخش چهارم را آغاز می‌کنیم: دفاع‌ها.*
